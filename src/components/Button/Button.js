import styles from './Button.module.scss';

const Button = props => {
    return (
        <div className={styles.button}>
            <a href="#">Galeria</a>
        </div>
    )
}

export default Button;